1) Как файлик XML может извлекать данные с сайта?
Моё понимание что я делаю инструкцию(экстрактор по шаблону),
 которую потом какая-то хрень обрабатывает. Это как бы карта где и что и как искать на сайте.

см. шаблон

2) Что значит проверить на закрытие по робот тхт.
открыть сайт и добавить /robot.txt
И что там надо найти в disable?


3) В HTML навожу на элемент и осознаю какой до него Хпаф
ясно.

4) Получается для каждой страницы свой спецробот? или вся инфа разбросана по сайту?

______
Шаблон (в примере) это и есть конфиг/ робот? как я понял.

5) "Обратите внимание что в кореневом элементе может быть только одна команда"
атрибут use-unique-entity-check="true" это и есть команда? Где вообще команды?

6) Не понимаю, что это за хрень
load - отправка запроса на сервер через Zora (балансер-приблуда для распределения нагрузки),
 а также с проверкой robots.txt

6.1) Где вообще load? в шаблоне, который дали.

7) Не понимаю, что это
 innermost-load - отправка запроса в обход Zora без проверки
  robots.txt (всегда используем innermost-load)

8) Оба элемента могут иметь параметр. Это про load and innermost-load?
Когда они пишут параметры это означает атрибуты?
Какие значения могут быть у priority?

9) Как попонять что эти параметры вообще нужны
Важными параметрами команд-запросов являются также заголовки и
 параметры отправляемого запроса.
  Эти значения объявляются в блоках <headers></headers>
  и <params></params> с помощью key-value параметров

10) Контекст надо бы пояснить эту телегу.

11) После того как концепция контекстов стала понятной,
 перечислим основные команды для работы с содержимым ответа на запрос:

12) Какой запрос и команды и содержимое ответа? Я ничего не отправлял.

13) куда вообще пишутся команды ? В Шаблон.

14) Зачем преобразовывать исходную вёрстку в JSON

15) Что они имеют ввиду под "сущность" и где вообще находиться поле сущности.

16) После того, как были найдены все пути до интересующих данных на сайте,
 можно приступать к написанию спецробота.

 Пути тупо выписать на бумажку/табличку?

 17) Первое на что надо обратить внимание - как сервер отдает и хранит данные.
 Как понять способ выдачи/хранения?

 Тип хранения данных:
 -Все в верстке
 -JSON
 -Структурированные данные

 18)
